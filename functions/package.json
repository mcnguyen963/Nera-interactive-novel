{
  "name": "nera-functions",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "echo 'Use vercel dev or netlify dev to run edge functions locally'",
    "deploy": "echo 'Deploy edge function to Vercel/Netlify'"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.3"
  }
}
